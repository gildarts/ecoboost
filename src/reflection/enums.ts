
/**
 * 定義所有支援的 reflect。
 */
export enum ReflectKey {
    /**
     * Service。
     */
    Service,
};

/**
 * Service 所支援的 http method。
 */
export enum ServiceMethod {
    All = 'all',
    Get = 'get',
    Post = 'post',
    Put = 'put',
    Delete = 'delete'
}